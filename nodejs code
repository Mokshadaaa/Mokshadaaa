export const handler = async (event) => {
  // TODO implement
  const name = "Ajaykumar Nadar"
  const response = {
    statusCode: 200,
    body: JSON.stringify(Hello ${name}),
  };
  return response;
};
